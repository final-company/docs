---
title: "WebSockets API"
description: "Reference for the public WebSockets REST API."
---

Use the WebSockets API to discover the hosted WebSocket URL, create short-lived session tokens, publish channel events, and offer work to connected workers.

API-key-backed session tokens are minted through the delegated credential lifecycle internally. Use [Delegated credentials](/api-references/delegated-credentials) when you need centralized lifecycle fields such as `id`, `refreshAfter`, and `refreshBefore`.

Every request uses API key authentication:

```bash
Authorization: Bearer <api-key>
```

Session creation, channel publishing, and work offers require a user-owned or team-owned API key. WebSockets resources are scoped to that key owner. A user-owned key and a team-owned key can use the same channel or queue names without sharing resources.

Send app-local resource names, such as `chat` or `customer-jobs`. Do not include user or team tenant prefixes in request bodies or paths.

The public base URL is:

```text
https://api.final.ai/v1/websockets
```

<Card title="Get metadata" icon="info" href="/api-references/websockets/meta">
  Read the protocol version and WebSocket URL.
</Card>

<Card title="Create a session" icon="key-round" href="/api-references/websockets/sessions/create">
  Mint a short-lived token with scoped channel, RPC, bridge, or work queue grants.
</Card>

<Card title="Publish a channel event" icon="send" href="/api-references/websockets/channels/publish">
  Publish an event to a WebSockets channel from the REST API.
</Card>

<Card title="Offer work" icon="briefcase-business" href="/api-references/websockets/work-queues/offer">
  Offer a work lease to workers connected to a queue.
</Card>

## Permissions

| Permission | Required for |
| --- | --- |
| `websockets:connect` | `POST /sessions` |
| `websockets:publish` | Channel publish grants and `POST /channels/{channel}/events` |
| `websockets:history` | Channel history grants |
| `websockets:presence` | Channel presence grants |
| `websockets:rpc` | RPC grants |
| `websockets:bridge` | Bridge grants |
| `websockets:work` | Work queue grants and `POST /work-queues/{queue}/offers` |
