---
title: "Durable Execution"
description: "Run event-driven workflows with durable steps, retries, waits, and run history."
---

Durable Execution runs long-lived workflows that can retry, sleep, wait for correlated events, and resume without losing completed step output.

Use it when a workflow needs to survive process restarts, external callbacks, human approval, or delayed work.

<Card title="Quickstart" icon="rocket" href="/durable-execution/quickstart">
  Register a workflow, send an event, inspect the run, and read history.
</Card>

<Card title="Workflows and events" icon="workflow" href="/durable-execution/workflows-and-events">
  Define event triggers, register workflow versions, and send one event or a batch.
</Card>

<Card title="Runs and signals" icon="activity" href="/durable-execution/runs">
  Trigger a workflow directly, inspect status, cancel, retry, and signal waiting runs.
</Card>

<Card title="API reference" icon="book-open" href="/api-reference/durable-execution">
  Browse every public REST endpoint for apps, workflows, events, and runs.
</Card>

## Core concepts

| Concept | Description |
| --- | --- |
| App | A durable execution namespace. The `appId` path segment identifies the app, such as `billing`. |
| Workflow | A registered durable workflow version with a runtime endpoint and one or more triggers. |
| Event | A named payload that can schedule matching workflows or resume waiting runs. |
| Run | One execution of a workflow. Runs can be `queued`, `running`, `waiting`, `completed`, `failed`, or `cancelled`. |
| Signal | A payload sent to a waiting run, optionally targeting a specific step. |

The public API is mounted under:

```text
https://api.final.ai/v1/durable-execution/apps/{appId}
```

For endpoint details, see the [Durable Execution API reference](/api-reference/durable-execution).
